{
    "builds": [
        {
            "src": "package.json",
            "use": "@vercel/static-build",
            "buildCommand": "vite build",
            "config": {
                "distDir": "dist"
            }
        }
    ]
}